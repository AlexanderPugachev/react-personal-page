import React from 'react';
import './Loading.css';

const Loading = () => {
  return (
    <div className="loading">
      Please, wait, web-app is loading
    </div>
  );
};

export default Loading;
